# APK 下载目录

把你构建好的 APK 放到这里,命名为:

```
PaperEcho.apk
```

网站上的下载按钮(`index.html` 中的 `href="./downloads/PaperEcho.apk"`)会直接命中此文件,点击即开始下载。

## 发布清单

- [x] `PaperEcho.apk` 已放入本目录
- [x] `index.html` 中版本号 / 包体积已更新
- [ ] (可选)在下方记录 SHA-256 供用户校验:

```text
SHA-256: 03503dbf39a3da1c2dc51e2d0215e801b0f9ffaf49f582109b8ab6b78e7b57cc
```



> 当前 APK 约 5.5 MB,可随站直接托管(GitHub Pages 单文件 100MB 限制内)。
