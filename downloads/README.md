# APK 下载目录

把你构建好的 APK 放到这里,命名为:

```
PaperEcho.apk
```

网站上的下载按钮(`index.html` 中的 `href="./downloads/PaperEcho.apk"`)会直接命中此文件,点击即开始下载。

## 发布清单

- [ ] `PaperEcho.apk` 已放入本目录
- [ ] `index.html` 中版本号 / 包体积 / 构建日期已更新(两处,已用 `★` 注释标出)
- [ ] (可选)在下方记录 SHA-256 供用户校验:

```text
SHA-256: 40b9802195ce5843da4299e59fd80307796a9f19e9e70b921b7eea70e6e48a54
```



> 当前 APK 约 3 MB,可随站直接托管(GitHub Pages 单文件 100MB 限制内)。
